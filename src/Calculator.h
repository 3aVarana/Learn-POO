class Calculator
{
private:
    /* data */
public:
    int sum(int x, int y);
    int minus(int x, int y);
    int multiplication(int x, int y);
    float division(int x, int y);
    int pow(int x, int y);
};
